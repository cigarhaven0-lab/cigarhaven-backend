services:
  - type: web
    name: cigarhaven-backend
    runtime: node
    buildCommand: npm install && npx playwright install chromium
    startCommand: node server.js
    envVars:
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: /opt/render/project/.playwright
